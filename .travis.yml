dist: xenial
language: minimal

addons:
  apt:
    packages:
      - awscli

services:
  - docker

before_install:
  - docker build -t $IMAGE .

script:
  - docker run --rm $IMAGE pytest

deploy:
  provider: script
  script: bash docker_deploy
  on:
    branch: master
