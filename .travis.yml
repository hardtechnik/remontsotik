dist: xenial
language: minimal

addons:
  apt:
    packages:
      - awscli

services:
  - docker

before_install:
  - docker build -t $IMAGE .

script:
  - bash docker_test

deploy:
  provider: script
  script: bash docker_deploy
  on:
    branch: master
